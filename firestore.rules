rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only our backend and authorized clients should access this.
    // For the sake of this prototype, allowing read/write.
    match /dossiers/{document=**} {
      allow read, write: if true;
    }
  }
}
