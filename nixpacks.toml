# Build = vite uniquement (dist/index.html). Runtime = tsx server.ts — jamais node dist/server.cjs
[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npx tsx server.ts"
