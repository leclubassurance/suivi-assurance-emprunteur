# dist/server.cjs n'est pas versionné — on build le frontend (vite) et on lance server.ts via tsx
[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npx tsx server.ts"
